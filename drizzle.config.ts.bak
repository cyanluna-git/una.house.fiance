import type { Config } from "drizzle-kit";

const config: Config = {
  schema: "./src/lib/db/schema.ts",
  out: "./drizzle",
  dialect: "sqlite",
  dbCredentials: {
    url: "file:./finance.db",
  },
};

export default config;
